# 基金挑战系统依赖
# 安装：pip3 install -r requirements.txt

# 核心依赖
akshare>=1.18.0        # 金融数据接口
requests>=2.31.0       # HTTP 请求库

# 可选依赖
pandas>=2.0.0          # 数据处理（可选，用于高级分析）
numpy>=1.24.0          # 数值计算（可选）

# 开发依赖
# pytest>=7.0.0        # 测试框架（未来添加测试时使用）
